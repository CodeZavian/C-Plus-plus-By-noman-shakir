#include <iostream> // preprocessor program ka pragram ka compile hone se phale jo bi preprocessing ha wo kr do
using namespace std;  // is ka without error through kre ga kuin na necehe bar abr sdt nahin lihkna pare ga 
int main()  
{
        // cout<<"hello world"<<endl;  // endl is for next line in c++ and ends is use for space
        // cout<<"next line";
        // variables;
        // int a,b,c ;    // variables are only satrt with letters and Underscore(_)
        short sa = 9; 
        short sb = 10;
        short sc = 12;
        cout<<sa<<endl;   // it print 9
        cout<<sb<<endl;  // it print 10
        cout<<sc<<endl; // it print 12


         // camel case notation  and variavles
    //   int MarkInMaths = 87;  // agar yeh capital houn ga to (mim) lihkne se aoutomatic suggest kr de ga
    //   cout<<"marks of noman in math = "<<MarkInMaths<<endl;

     // string noman = "noman"

     // varables
    //  short a;
    //  int b;
    //  long c;
    //  long long d;


   
     float  const score = 43.44; // now if you change value in furthers next step it gives you error
     // you cant change due to using (const)
//      double score1;
//      long double score2;

     //score = 23.3; you can a re - assingn the  value
     cout<<"the score is "<<score<<endl;
  

      // data type casting ha yeh round off nahin kare ga if 12.9 so it will print 12 in int
     // int a = 213;
     // cout<<(float)a/32<<endl;
     // float b = 23.23;
     // cout<<(int)b;




        return 0;
}